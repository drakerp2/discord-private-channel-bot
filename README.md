# discord-private-channel-bot
a discord bot that makes a private channel for each member of the server

pip requirements:
discord.py

planned features:
container deployment (probably with distrobox-arch)
